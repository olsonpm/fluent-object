'use strict';


//---------//
// Imports //
//---------//

var <%= rawFoInst.ConstructorName() %> = require('../base/<%= rawFoInst.ConstructorName() %>');
<% rawFoInst.Properties()
    .filter(function(foPropertyInst) {
        return foPropertyInst.Flags().contains(FOProperty.FLAGS.validate_instance);
    })
    .each(function(foPropertyInst) { %>
        
//---------------------//
// Extended Validation //
//---------------------//

<%= rawFoInst.ConstructorName() %> .prototype._Validate <%= foPropertyInst.Name() %> = function _Validate <%= foPropertyInst.Name() %> (input, throwErr) {
    throw new Error("Not Implemented Yet");
};

<% }) %>
//---------//
// Exports //
//---------//

module.exports = <%= rawFoInst.ConstructorName() %>;
